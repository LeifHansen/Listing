# The image serves dist/ and the two nginx files. Nothing else belongs in the
# build context — node_modules alone is ~300MB of upload per deploy.
node_modules
src
public
assets
scripts
.astro
README.md
package.json
package-lock.json
astro.config.mjs

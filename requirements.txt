fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
Pillow==12.3.0
pillow-heif==1.7.0
anthropic==1.4.0
pydantic==2.13.5
python-dotenv==1.2.3
httpx==0.28.1
SQLAlchemy==2.0.52
# Schema migrations. Not on the boot path yet -- the app still calls
# create_all plus the guarded ALTERs -- but shipped in the image so the
# cutover is `alembic stamp head` then `alembic upgrade head` on the box
# rather than a redeploy. See alembic/env.py.
alembic==1.19.2
psycopg[binary]==3.3.5
bcrypt==5.0.0
PyJWT==2.13.0

# Encrypts the marketplace refresh tokens at rest (backend/crypto.py, imported
# by backend/db.py -- the app cannot start without it). Declared explicitly for
# the same reason numpy is below: pip resolves it today only as an OPTIONAL
# extra of PyJWT/oauthlib, which nothing here requests, so a clean install had
# every right not to have it.
cryptography>=50.0.1
boto3==1.43.89
# rembg's own dependency, declared as a floor rather than a pin so pip is free
# to resolve whatever onnxruntime and rembg want.
numpy>=2.5.3

# In-house background removal: rembg pulls in onnxruntime + a ~176MB
# isnet-general-use model (baked into the image at build time, see Dockerfile).
rembg==2.0.83
onnxruntime==1.29.0

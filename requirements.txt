fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
Pillow==11.1.0
pillow-heif==0.21.0
anthropic==0.45.0
pydantic==2.10.5
python-dotenv==1.0.1
httpx==0.28.1
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
bcrypt==4.2.1
PyJWT==2.10.1
boto3==1.35.99
# Declared explicitly, not left to rembg to drag in: services/matte.py imports
# numpy at module scope and services/images.py imports matte, so the app cannot
# START without it. Relying on a transitive dependency for that meant the boot
# requirements were a fiction -- strip rembg (as the CI smoke job does, and as
# any slimmer deployment would) and the app stops importing. Deliberately a
# floor rather than a pin, so pip is free to resolve whatever onnxruntime and
# rembg want and this can never wedge the image build.
numpy>=1.26

# In-house background removal: rembg pulls in onnxruntime + a ~176MB
# isnet-general-use model (baked into the image at build time, see Dockerfile).
rembg==2.0.59
onnxruntime==1.20.1
